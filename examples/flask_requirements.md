# Flask Day Server

Build a simple Python Flask web server that shows the current day.

- Within a virtualenv
- Index page at / displays "Today is [day of week]"
- Simple HTML with basic styling
- Run on port 8000